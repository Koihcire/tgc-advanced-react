import React from 'react'

const StudentContext = React.createContext({
//if the data in the context is hardcoded and will not change, can put it here. Otherwise, put it in the provider in app.js
});

export default StudentContext;